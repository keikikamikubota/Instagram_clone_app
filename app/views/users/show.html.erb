<h1><%= @user.name %>のページ</h1>

<p>名前: <%= @user.name %></p>
<p>メールアドレス: <%= @user.email %></p>

<% if @user.id == current_user.id %>
  <%= link_to 'Edit', edit_user_path(@user.id), class: "btn btn-light" %>
<% end %>

<%= link_to 'Back to TimeLine', pictures_path, class: "btn btn-light" %>